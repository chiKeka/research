[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Pretty 404s for unknown paths (Astro emits 404.html).
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
