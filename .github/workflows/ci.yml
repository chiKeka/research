name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install
        run: npm ci

      - name: Type-check (astro check)
        run: npm run check

      - name: Unit tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Ledger freshness (warns; pass --strict to gate)
        run: npm run ledger:fresh

      - name: Link check (built site)
        uses: lycheeverse/lychee-action@v2
        with:
          # --root-dir lets lychee resolve root-relative links (/ledger, /about)
          # in the local dist; github.workspace is the trusted runner path.
          args: >-
            --no-progress
            --root-dir ${{ github.workspace }}/dist
            --max-retries 4
            --timeout 30
            --accept 200,202,206,403,429
            --exclude '://(www\.)?(x|twitter|linkedin)\.com/'
            './dist/**/*.html'
          # External link reachability is flaky and outside our control: a slow
          # upstream server timing out is not a broken site. So the link check is
          # advisory - the full report still lands in the job summary, but a
          # transient external timeout no longer reds the build. Broken INTERNAL
          # links still surface in the report; gate them with a separate
          # --offline step if a hard internal-link gate is wanted.
          fail: false

  lighthouse:
    runs-on: ubuntu-latest
    needs: verify
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Lighthouse CI (budget assertions)
        # Reports scores and asserts the budget. Left non-blocking until the
        # thresholds are confirmed against a few real runs; flip to a hard gate
        # by removing continue-on-error and raising the assertions to "error".
        continue-on-error: true
        uses: treosh/lighthouse-ci-action@v12
        with:
          configPath: .github/lighthouse/lighthouserc.json
          uploadArtifacts: true
          temporaryPublicStorage: true
