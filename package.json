{
  "name": "bruno-chikeka-research-hub",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prep": "node scripts/prep-data.mjs",
    "dev": "node scripts/prep-data.mjs && astro dev",
    "build": "node scripts/prep-data.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test",
    "ledger:fresh": "node scripts/check-ledger-fresh.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.8"
  },
  "overrides": {
    "esbuild": "^0.28.1",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^6.0.3"
  }
}
